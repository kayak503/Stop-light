<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="legos/style.css">
    <script src="legos/main.js"></script>
    <title>Webapp</title>
</head>
<body>
    <div class="light-toggle">
        <div class="div-button 
        <% if (red == false) { %>
            <%="clicked" %> 
        <% } %>
        " id="RED"></div>
        <div class="div-button
        <% if (yellow == false) { %>
            <%="clicked" %> 
        <% } %>
        " id="YELLOW"></div>
        <div class="div-button
        <% if (green == false) { %>
            <%="clicked" %> 
        <% } %>
        " id="GREEN"></div>
    </div>
</body>
</html>